UI for changing sites

INSTALLATION:

    git clone https://github.com/venturecommunism/sites-ui.git

    cd sites-ui

    ln -s /path/to/sites-ui/initialize.js /usr/bin/sites-ui

    cd /path/to/web/root

Edit variables.config
Save

    sites-ui
